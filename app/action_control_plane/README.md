# action_control_plane Package

This path remains reserved for future domain modules once the control-plane business APIs start landing.

The initial runtime foundation lives in sibling modules under `app/` so the service can boot cleanly before the larger domain package is introduced.

Expected subdomains:

- `intake`
- `tenancy`
- `policy`
- `approvals`
- `evidence`
- `receipts`
- `proofs`
- `signing`
- `escrow`
- `audit`

These names are placeholders, not implemented modules.
