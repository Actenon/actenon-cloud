# Migrations

This directory contains Alembic migration scaffolding for Actenon Cloud.

Current contents:

- `env.py`
- `script.py.mako`
- `versions/`

No business migrations exist yet because the runtime foundation stops short of implementing domain tables.
